import { AppLoadingShell } from "@/components/ui/AppLoadingShell";

export default function Loading() {
  return (
    <AppLoadingShell
      title="Preparing your ShopBridge storefront"
      subtitle="Loading live products, categories, and checkout data for a smoother browsing experience."
    />
  );
}
